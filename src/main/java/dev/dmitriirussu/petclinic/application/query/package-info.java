/**
 * Query slice of the application layer.
 *
 * <p>Contracts prefixed with {@code Ssr} are specific to the
 * server-side rendered (SSR) transport. Contracts without the
 * prefix are transport-independent.
 */
package dev.dmitriirussu.petclinic.application.query;